# Bible Pro

> 简洁、易用的圣经经文阅读与投影软件

**Bible Pro** 是一款基于 **Python + PyQt6 + SQLite** 开发的 Windows 桌面端圣经经文阅读与投影工具，适用于教会聚会、礼拜、讲道、查经等场景。

软件采用 **主屏控制 + 副屏投影** 的工作方式：主屏负责选择书卷、章节和经文范围，并提供实时预览；副屏全屏显示经文，适合连接第二台显示器、电视或投影仪使用。

## ✨ 主要功能

### 📖 经文选择

- 内置《和合本》SQLite 经文数据库
- 支持旧约、新约及书卷简称选择
- 支持章节、起始节、结束节选择
- 只输入书卷和章节时，可直接进入该章的经文选择
- 未填写节号时，可直接显示整章
- 只填写起始节时，可显示至本章末节
- 支持连续显示与按节分段显示
- 支持快速调整起始节和结束节

### 🔎 快速搜索

通过书卷简拼快速定位经文，搜索框会根据输入内容智能匹配书卷、章节和经文范围。

- 支持书卷简拼逐步匹配
- 多个候选书卷时可继续输入或使用方向键选择
- 唯一匹配时自动完成书卷识别
- 简拼无法继续匹配时自动转换为文字
- 支持章节、节号及起止范围输入
- 支持空格等分隔符进行范围输入
- 支持 `↑ / ↓` 切换候选项
- 支持 `Space` 选择候选
- 支持 `Enter` 确认搜索
- 支持 `Backspace` / `Delete` 修改输入
- 限制无关字符输入，减少误操作

### 🖥️ 双屏投影

- 支持第二显示器、电视或投影仪
- `F12` 开启 / 关闭扩展显示
- `Esc` 关闭扩展显示
- 自动选择非主屏作为投影屏
- 副屏全屏显示经文
- 根据副屏分辨率自动排版
- 主屏预览与副屏显示同步
- 主屏滚动位置同步到副屏

### 📜 经文滚动

- 支持连续显示和按节分段显示
- 支持手动滚动和自动滚动
- 自动滚动速度支持 **1～9 档**
- `0` 暂停自动滚动
- `Space` 暂停 / 继续自动滚动
- `↑ / ↓` 手动滚动
- 支持鼠标滚轮滚动
- 滚动到末尾后自动停止

### 🎨 显示设置

支持自定义经文及投影画面的显示效果：

- 正文字体、字号、颜色
- 标题字体、字号、颜色
- 节号字体、字号、颜色
- 底注字体、字号、颜色
- 行距与左右边距
- 底注区域高度
- 背景颜色
- 背景图片
- 亮色 / 暗色控制台主题

显示设置会自动保存，下次启动时恢复。

### 🕘 历史记录

- 自动记录已打开的经文
- 未打开的内容不会加入历史记录
- 可快速重新打开历史经文
- 每条历史记录支持单独删除
- 支持复制历史记录中的经文标题或简称
- 支持一键清空历史记录

## ⌨️ 快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Enter` | 打开 / 关闭快速搜索或确认搜索 |
| `F12` | 开启 / 关闭扩展显示 |
| `Esc` | 关闭扩展显示 |
| `←` / `→` | 减少 / 增加结束节 |
| `Ctrl + ←` / `Ctrl + →` | 减少 / 增加起始节 |
| `Space` | 暂停 / 继续自动滚动；搜索时选择候选项 |
| `1` ～ `9` | 设置自动滚动速度 |

> 当焦点位于搜索框、数字输入框或其他控件时，相关按键优先由当前控件处理。

## 📦 安装使用

### Windows 安装版

前往 GitHub **Releases** 页面下载最新安装程序，运行安装包并按照提示完成安装即可。

### 源码运行

环境要求：

- Windows
- Python 3.x
- PyQt6

安装依赖：

```bash
pip install PyQt6
```

启动程序：

```bash
python main.py
```

运行时需要确保 `和合本.db`、`icon.ico`、`styles` 等资源位于项目目录。

## 🖥️ 扩展屏使用

将电脑连接到投影仪、电视或第二台显示器，并将 Windows 显示模式设置为 **扩展这些显示器**。

启动 Bible Pro 后：

1. 在主屏选择需要显示的经文。
2. 按 `F12` 开启扩展显示。
3. 副屏将全屏显示经文。
4. 主屏的经文切换和滚动会同步到副屏。
5. 按 `Esc` 关闭扩展显示。

## 🛠️ 技术栈

| 技术 | 用途 |
| --- | --- |
| Python | 主要开发语言 |
| PyQt6 | GUI、窗口、快捷键及双屏显示 |
| SQLite | 经文及书卷数据存储 |
| QTextDocument | 经文排版与显示 |
| QSS | 界面主题和控件样式 |
| PyInstaller | Windows EXE 打包 |
| Inno Setup 7 | Windows 安装程序制作 |

## 📁 项目结构

```text
bible_projection/
├── main.py                    # 程序入口
├── main_window.py             # 主窗口及核心业务逻辑
├── bible_database.py          # 数据库及经文查询
├── config.py                  # 本地配置、显示设置、历史记录
├── 和合本.db                  # 经文数据库
├── icon.ico                   # 应用图标
├── install.mark               # 安装版识别标记
├── styles/                    # 界面样式
├── ui/                        # 界面与显示模块
├── Bible Pro.spec             # PyInstaller 打包配置
├── build_exe.bat              # 一键生成 EXE
├── Bible Pro.iss              # Inno Setup 7 安装程序配置
├── file_version_info.txt      # Windows 文件版本及版权信息
├── LICENSE                    # 项目许可
└── README.md
```

## 📦 Windows EXE 打包

项目提供 `build_exe.bat`，使用 PyInstaller 生成 Windows EXE，不负责制作安装程序。

在项目根目录运行：

```text
build_exe.bat
```

打包完成后生成：

```text
dist/
└── Bible Pro/
    ├── Bible Pro.exe
    ├── 和合本.db
    ├── icon.ico
    ├── install.mark
    ├── styles/
    └── 其他运行依赖
```

用户配置、搜索历史和窗口状态等数据不会被打包进 EXE。

## 📦 制作 Windows 安装程序

使用 **Inno Setup 7** 和项目中的 `Bible Pro.iss` 制作最终安装程序。

基本流程：

1. 运行 `build_exe.bat`。
2. 确认 `dist\Bible Pro\` 中的程序可以正常运行。
3. 使用 Inno Setup 7 打开 `Bible Pro.iss`。
4. 确认安装文件指向 `dist\Bible Pro\`。
5. 点击 **Build → Compile**，或按 `Ctrl + F9`。
6. 在 `Output` 目录获取安装程序。

最终安装包类似：

```text
Output/
└── Bible Pro_Setup.exe
```

安装包只应包含程序运行所需文件，**不要将用户配置文件加入安装包**。

## ⚙️ 配置与用户数据

Bible Pro 会保存显示设置、窗口状态和历史记录。安装版运行时，用户配置与程序安装目录分离，避免升级或重新安装时直接覆盖用户数据。

| 文件 / 数据 | 作用 | 随安装包提供 |
| --- | --- | --- |
| `和合本.db` | 经文数据库 | ✅ |
| `icon.ico` | 应用图标 | ✅ |
| `styles/` | 界面样式 | ✅ |
| `install.mark` | 安装版识别标记 | ✅ |
| 用户配置 | 显示设置、窗口状态、历史记录等 | ❌ |

## 🐛 问题反馈

如果使用过程中遇到问题或有功能建议，欢迎通过 GitHub **Issues** 提交反馈。

提交问题时建议提供：

1. Bible Pro 版本
2. Windows 系统版本
3. 问题描述
4. 问题出现的操作步骤
5. 截图或错误信息（如有）

## 📄 版权与许可

**Bible Pro**

```text
Copyright © 2026 XiaoDong
```

项目软件代码的许可协议请参阅 `LICENSE` 文件。

项目中的《和合本》经文及数据库内容可能涉及相应版权和授权问题。使用、传播、修改及二次开发时，请遵守相关版权规定及适用法律法规。

---

**Bible Pro** · **Python + PyQt6 + SQLite**
